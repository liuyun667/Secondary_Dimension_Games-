# 必装（Web 服务）
fastapi>=0.110
uvicorn[standard]>=0.29

# ── 可选：真实 OCR（不装则用 text_lines 模拟输入）──
# paddleocr>=2.7
# paddlepaddle>=2.6

# ── 可选：fasttext 粗分类 / sklearn 随机森林评分 / BERT 实体链接 ──
# fasttext>=0.9.2
# scikit-learn>=1.3
# torch>=2.1
# transformers>=4.38

# LLM 解释走 DeepSeek API，使用 stdlib urllib，无需额外包
