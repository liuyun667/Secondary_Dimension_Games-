# 二游毕业指导 · 变更记录（CHANGELOG）

## v1.9.2（当前）

### 优化
- **进入网站默认为空**：移除启动时自动加载已保存资产（boot/切游戏均不再请求 `/assets`）——页面打开显示「暂无资产」，侧边栏显示「暂时没有任何资料，请导入」；数据仅在显式操作后得到：点「导入测试数据」实时生成，或识别/导入后点「保存我的资产」留存
- **局域网访问**：服务绑定 `0.0.0.0`（`启动.bat` 同步更新），局域网内任意设备访问 `http://<本机IP>:8000/`
- **文档页 `/docs`**：零依赖 markdown→HTML 渲染器（`app/md_render.py`）渲染三个文档（项目介绍/技术文档/数据采集决策树），侧边导航切换；Swagger 挪到 `/api/docs` 避免路径冲突
- **工作区整理**：新增 `_archive/` 归档区（`gen_random_data.py`、`sample_ocr.txt`、`_api_smoke_report.txt`），附归档说明 README
- **文档重写**：`二游毕业指导.md`（技术文档）、`二游毕业指导-项目介绍.md`（项目介绍）更新至 v1.9.1（66 角色/150 武器/400 材料/四类保存/专属 3 档毕业/局域网访问）

## v1.9.1（上一版）

### 修复
- **毕业属性置灰不生效**：`applyKeyStats` 用 `querySelector` 查滑块时漏写 `#` 前缀（`$("stdCr")` 被当成标签选择器返回 null），异常被 catch 吞掉导致滑块从不置灰。已修复为 `$("#" + id)` 并用**无头 DOM 测试**（jsdom + 真实后端）验证：胡桃的防御力/攻击力/充能正确置灰，暴击/爆伤/生命/精通可调

### 优化
- **导入按钮文案**：去掉「导入测试数据」后括号内的规模说明，仅保留按钮名
- **每角色专属关注属性（key_stats）**：为全部 66 位原神角色配置精确的「关注属性」表（`KEY_STATS`，口径参考米游社/B站毕业攻略）——主C看暴击/爆伤/攻击（或生命），副C加充能，辅助看生命/防御/精通/充能；**无用属性（如胡桃的防御力）在毕业标准中自动置灰、不参与评估**，例如：
  - 胡桃：暴击率/暴击伤害/**生命值**/元素精通（防御力、攻击力、充能置灰）
  - 钟离：生命值/暴击率/暴击伤害/充能；诺艾尔/希诺宁：**防御力**/暴击/爆伤/充能
  - 行秋：充能/暴击/爆伤/攻击；砂糖/万叶：精通/充能/暴击

## v1.9（上一版）

### 新增
- **「导入测试数据」一键生成**（合并原「导入示例账户/载入测试账户」为单个按钮）：
  - 点击时**后端实时生成**（`POST /api/v1/test-data/generate`，时间戳=点击时刻），规模：**66 角色**（5星 37 ≥36、等级 70~90）、**150 武器**（5星 50 ≥50、等级 70~90、含精炼/基础攻击/主词条/被动）、**1000 圣遗物**（不同套装×部位、等级 16~20、主副属性带数值）、**400 材料**（升级相关 240 ≥200，含数量+来源）
  - 生成约 7.5s，1616 项资产评估仅 0.08s
- **知识库扩充 v6**：角色 38→66（新增稻妻/须弥/枫丹/纳塔 28 位+配装）、武器 100→150（新增 7 把确认 5★：苍耀/焚曜千阳/赤月之形/香韵奏者/尘世之锁/冲浪时光/祭星者之望 + 4★/3★）、材料知识库 400 种（`app/kb_materials.py`，真实名+来源）
- **数据真实性说明**：角色/武器/材料名称与星级为真实数据（部分专武经 web 核实：苍耀=丝柯克、焚曜千阳=玛薇卡、祭星者之望=茜特菈莉）；新增 5★ 被动文本标注"以游戏内为准"；少数新角色 boss 材料为社区口径占位

## v1.8.1（上一版）

### 新增
- **资产详情弹窗**：资产列表每项可点击（hover 高亮 + 光标提示），弹出详情查看完整信息——角色（星级/等级/命座/技能等级）、武器（类型/精炼/基础攻击/主词条/被动）、圣遗物（套装/部位/主属性/副属性/达标评价）、材料（数量/来源），附识别置信度与原文本；点击遮罩或关闭按钮退出
- **侧边栏账户资产汇总**：侧边栏底部新增「账户资产」卡片，按 角色/武器/圣遗物（声骸/驱动盘随游戏切换）/材料 四类显示数量 + 合计；无资产时显示「暂时没有任何资料，请导入」，资产增删即时刷新
- **顶栏固定**：顶栏改为 `position: fixed`（原 sticky 在部分布局下会随内容滚走），主内容任意滚动时顶栏始终可见
- **4 套配色主题**：蓝紫（默认）/ 森林绿 / 樱花粉 / 暗夜，顶栏下拉切换，localStorage 记忆；输入控件背景变量化以适配暗色
- **毕业标准只看主要属性**：`graduate` 返回该角色 `key_stats`（毕业标准关注的副词条），前端把非主要属性滑块**置灰禁用**（标注"该角色不要求"），提交时跳过 → 评估/评分不关注这些属性；用户仍可用「自定义词条」自由添加其他属性

## v1.8（上一版）

### 新增
- **资产四类分开保存**（角色/武器/圣遗物/材料，三游戏字段切换）：
  - `user_asset` 新增 `type` 列（幂等迁移，保留旧数据并自动反查补全）
  - 武器格式：名称/星级/类型/等级/**精炼**/基础攻击/主词条(如暴击伤害66.2%)/被动；圣遗物：部位/套装/等级/星级/主属性(含数值)/副属性(含数值)；材料：名称/数量/**来源**；角色：名称/星级/等级/命座/技能等级(普攻/战绩/爆发)
  - 新增 `kb_weapon` 武器固有属性知识库（类型/基础攻击/主词条/被动）
- **每角色专属 3 档毕业要求**（休闲/标准/极限）：`criteria.tiers` 逐角色差异化（等级/武器/副词条目标/技能等级逐档提升），合并优先级：角色专属档 > 全局模板 > 默认标准
- **测试账户数据**（`scripts/gen_test_data.py`）：30 位角色（含纳塔恰斯卡/玛薇卡/希诺宁等）+ **100 件真实武器**（`app/kb_weapons.py`，含星鹫赤羽608/66.2%/眸中的月珥）+ **1000 件圣遗物**（19 套×5 部位×主副词条）+ 材料（含来源）→ 写入 `test_demo` 账户；网页新增「载入测试账户」一键体验
- **种子扩充 v5**：角色 12→30、装备套装 5→19（黑曜秘典/逐影猎人/黄金剧团等）、武器知识库 100 件、配队新增纳塔队

## v1.7.2（上一版）

### 修复
- **云游戏/窗口标题校准**：`--calibrate` 现在可独立使用（不必带 `--auto-detail`）；找不到窗口时列出**当前所有可见窗口标题**，方便云游戏用户抄客户端窗口名（云游戏窗口标题是客户端名如「网易云游戏/START」，不是游戏名）；`_find_window` 提示同步增强
- **自动点击依赖缺失**：`tools/auto_export.py` 的 `import pyautogui/pygetwindow` 改为 `_need()` 懒加载，缺失时提示安装命令（`pip install pyautogui pygetwindow`）而非裸 Traceback；toumanfen 环境已安装
- **日志编码乱码导致状态误判**：Windows 下子进程 stdout 重定向到文件按 GBK 写，父进程按 UTF-8 读 → "未找到游戏窗口"等检测词匹配不上。修复：子进程注入 `PYTHONIOENCODING=utf-8` + 日志读取 UTF-8/GBK 双编码容错
- **载入采集结果体验**：文件不存在时提示完整解决路径（先开启自动点击并等待完成/核对输出文件名）；前端载入失败时展示上次采集日志尾部（真实原因，如"未找到游戏窗口「xxx」"）

### 新增
- **画面区域 viewport 支持（浏览器/云游戏专用）**：`--viewport x,y,w,h`（窗口内百分比）把点击坐标与截图裁剪限定到游戏画面区域——浏览器非全屏时标签栏/地址栏占掉顶部，默认坐标会点到浏览器 UI；F11 全屏可留空。CLI（`--viewport`）、网页（伴生工具卡片新增「画面区域」输入框）、后端（`AutoClickReq.viewport` 透传+校验）三端打通
- **网页一键开启自动点击**（读屏面板内嵌「伴生工具自动点击」卡片）：
  - `POST /api/v1/auto-click/start` 用 subprocess 启动 `tools/auto_export.py --auto-detail`（参数白名单 + 输出路径限定项目目录，防注入）；`GET /status` 实时返回进度/已收集件数/完成状态（解析日志）；`POST /stop` 终止（断点已落盘，可续采）
  - 前端：窗口标题/输出文件/最大件数/断点续采复选框，开启→轮询进度→完成后「载入采集结果」一键导入资产清单
  - 新增 `auto` 采集插件（`app/collectors/auto_collector.py`）：结构化结果直转资产，不经文本行二次解析
- **毕业标准自定义支持技能等级目标**（普通攻击 / 元素战绩 / 元素爆发，各 1~10 级，默认 9/9/9）：
  - 前端「毕业标准」新增三个技能等级滑块，随其他标准一起实时预览、保存、恢复默认
  - 后端 `criteria.talents`（normal/skill/burst）随 overrides 深度合并；毕业度「天赋」维度按目标逐项评估，差距清单精确到技能（如"天赋·普通攻击不足（当前 9/目标 10）"）
  - 培养建议新增「技能目标」行，技能升级材料缺口按目标等级折算（目标 8/8/8 比 9/9/9 材料更少）
  - 种子数据 `kb_graduation.criteria` 增加默认技能目标字段（老库由评估逻辑兜底，无需重建）

## v1.7.1（上一版）

### 新增
- **数据采集决策树文档**：`二游毕业指导-数据采集决策树.md`——六种采集方式（示例账户/粘贴/截图/读屏/导出文件/伴生工具自动点击）的适用场景、精确度、前置条件与选择流程图（mermaid）
- **伴生自动导出器正式版**（`tools/auto_export.py`）：
  - **断点续采**：每件采集后实时写 `<输出>.state.json`（含已采数据本体），Ctrl+C/异常中断后 `--resume` 继续，不丢数据
  - **进度与 ETA**：每 10 件打印 进度/已收集条数/预计剩余时间
  - **容错停机**：连续 N 件识别失败自动停止（`--max-miss`，默认 10），防卡死/防越界
  - 修复 `os` 导入作用域错误（`_load_state`/`_save_state` 原会在运行时 NameError）
- **API 冒烟测试脚本**：`scripts/api_smoke.py`——UTF-8 直连接口，覆盖 health/games/sample/analyze/graduate/advice 全链路（13 项断言），规避 PowerShell GBK 编码对中文 JSON 的破坏

## v1.7（上一版）

### 增强
- **鸣潮/绝区零专属材料配方**：鸣潮（贝币/角色经验材料/属性突破材料·气动·冷凝·热熔·导电/技能强化材料/武器经验材料）、绝区零（丁尼/角色经验芯片/属性突破芯片·物理·火·冰·雷·以太/技能强化芯片/音擎能源），按角色属性命名
- **队伍整体刷取优先级**：多人培养建议末尾新增【队伍刷取优先级】，聚合全队材料缺口并按档排序（周本→BOSS 突破→天赋书→日常），标注每项材料涉及的角色
- **刷取优先级分类修正**：BOSS 突破素材（未熟之玉 等）从"日常"档修正到"首领/BOSS"档
- **API Key 配置文件管理**：新增 `.env` / `.env.example`（启动时自动加载，环境变量优先，零依赖实现）；README 说明更新

## v1.6

### 增强
- **材料配方真实化**：升级配方改用社区共识数值——角色 1→90（大英雄的经验×417 / 首领突破素材×46 / 摩拉≈165万）、天赋三满（天赋书 教导9·指引63·哲学114 / 周本×18 / 摩拉≈496万）、武器 1→90（精锻用魔矿×605 / 摩拉≈75万）
- **每角色专属材料**：原神 20 角色配专属天赋书 / 首领突破素材 / 区域特产（`CHAR_MATERIALS`，如 胡桃=「勤劳」/未熟之玉/霓裳花）；鸣潮/绝区零用通用材料（TODO 补全）
- **培养建议接入 LLM 润色**：`llm_polish_advice()` 用 DeepSeek 把结构化建议改写成自然、专业、可复制的文本；无 key / 失败自动降级模板（已用真实 key 实测）
- **材料刷取优先级**：培养建议新增「刷取优先级」段落（周本→BOSS 突破素材→天赋书→日常材料）
- **项目介绍文档更新**：功能一览与现状总结补齐（培养建议 / 材料缺口 / 角色详情 / 配对人数 / 档位贯穿 / OCR 引擎）

## v1.5

### 新增（培养建议与游戏差异化）
- **培养建议生成器**（`POST /api/v1/advice` + 前端「生成培养建议」按钮）：按所选角色（1~9 人）+ 毕业档位输出**可复制文本**——升级路径、角色/技能/武器材料缺口、技能加点顺序、配装、差距清单
- **材料配方库** `kb_recipe`（角色升级/技能升级/武器升级，演示级按稀有度公式生成，seed v4）+ **材料缺口计算**（`material_gaps`）
- **材料数量解析**：`摩拉 x999999` → 名称+数量，资产与缺口计算可用
- **角色详情识别**：命座 / 天赋等级（`命座1`、`天赋 9 10 10`），参与天赋维度评估
- **毕业档位贯穿**：休闲/标准/极限决定等级目标（休闲→80 级）→ 材料缺口按目标折算
- **技能加点按角色配置**：`TALENT_PRIORITY`（全部加点 / 主Q），培养建议给出加点顺序
- **配对人数按游戏区分**：`game.max_team_size`（原神/鸣潮=4，绝区零=3），前后端按游戏校验

## v1.4

### 修复（OCR 引擎更换）
- **OCR 截图识别改用 RapidOCR（onnxruntime）**：本机 paddlepaddle 3.3.1 CPU 推理存在 PIR/oneDNN 已知 bug（`ConvertPirAttribute2RuntimeAttribute`），导致截图识别报错；RapidOCR 不依赖 paddle/torch，识别正常
- 代码改为 **RapidOCR 优先、paddle 兜底**（`app/pipeline.py`、`tools/auto_export.py`）
- paddlepaddle 曾临时降级 3.0.0 测试（仍有 PIR bug + torch 桥接问题），已还原 3.3.1
- 后端识别/采集接口捕获所有异常并返回**真实错误原因**（不再笼统 500/「请求失败」）
- 前端错误提示显示真实原因；读屏页提示"首次识别需下载模型"

### 新增（毕业标准自定义增强）
- 滑杆范围调整：暴击率 0~100、暴击伤害 0~400、角色/武器等级 70~100
- 新增滑杆：元素充能效率 100~400%、元素精通 0~2000、攻击力 0~6666、防御力 0~6666、生命值 0~100000
- 「自定义词条」：自由输入任意词条名 + 目标数值，合并进自定义标准（会话级）

## v1.3

### 修复（基于漏洞评审）
- **组A 性能+解析**：fasttext 模型模块级缓存（不再每行加载）；主词条支持无括号格式（`生命值 +4780`）；部位词覆盖「理之冠/冠」等真实名称；主词条判定按套装过滤（不再把别的套装混入）
- **组B 毕业度判定修正**：副词条改为**单件达标滚**判定（数值 ≥ `SUBSTAT_ROLL_MIN` 达标线），废除跨装备数值累加；阈值/权重集中到 `app/constants.py`
- **组C 知识库扩充**：原神 12→20 角色、12→36 武器、5→30 套装（含配装/别名）；鸣潮/绝区零武器与套装扩充；seed v3
- **组D 标准/合并语义**：自定义标准支持 `_append` 追加（如 `weapon.best_append`）；多页扫描重复项**刷新数据**而非丢弃
- **组E 前端健壮性**：全局 `unhandledrejection` 友好错误提示；CORS 收紧到本机（localhost/127.0.0.1）

## v1.2

### 新增
- **数值解析**（`app/pipeline.py` `structure()`）：主词条带数值（`（生命值 4780）`）、副词条结构化（`暴击率+7.8%` → `{name, value, percent}`）、圣遗物强化等级（独立 `+20`）
- **精确副词条达标度**（`app/engine.py` `substat_status()`）：按角色毕业标准逐词条判定 达标/未达标/未出现/未识别，毕业度与配装推荐结果卡均展示标签（跨装备累加数值）
- **采集插件系统**（`app/collectors/`）：file / clipboard / sample / ocr 四种采集方式，`POST /api/v1/collect`；新增采集来源只需注册一个插件
- **伴生自动导出器**（`tools/auto_export.py`）：自动翻页截图 → PaddleOCR → 卡片解析 → 导出 JSON；含 `--text-lines` 离线测试模式与 `tools/sample_ocr.txt`、`tools/sample_ocr_20.txt` 样例
- **前端**：资产识别页「自动导入」模式（选导出文件/粘贴）；资产卡显示副词条达标度标签；「导入示例账户」一键演示；「清空当前资产」按钮

### 修复
- 读屏/截图识别结果由"覆盖"改为"追加合并"（按类型|名称去重），支持多页扫描
- 定时采集防重入（上一帧未识别完不发下一帧）
- 材料关键词扩充（结晶/残渣/丁尼/提取物等）
- fasttext 模型路径迁移到 ASCII 路径（Windows 下 C++ 无法写入含中文路径）

## v1.1

### 新增
- **三游戏**：原神（12 角色）/ 鸣潮（10 角色）/ 绝区零（10 角色），各带武器/装备/配装/毕业标准/配队
- **开局选游戏**：首次进入选择游戏并记住
- **账户数据**：资产保存 / 删除（`/api/v1/assets`）
- **多人配装**：同时 1~9 人批量生成（`/api/v1/recommend/batch`）
- **多队伍配置**：1~4 队，每队 2~4 人（`/api/v1/teams/recommend`）
- **毕业标准自定义**：标准/休闲/极限/自定义四档 + 滑杆实时对比「系统 vs 我的标准」
- 实体类型统一为 角色/武器/装备/材料，装备称谓随游戏切换（圣遗物/声骸/驱动盘）
- seed 版本化重建（`SEED_VERSION`）

### 修复
- 前端事件绑定与数据加载解耦（资产读取失败不再中断按钮初始化）
- 静态资源加版本号（`?v=3`）防缓存

## v1.0

### 首个可运行版本（原神单游戏）
- 资产识别：读屏（getDisplayMedia）/ 上传截图 / 粘贴文本 → OCR → 分类 → 实体链接 → 属性结构化
- 毕业度评估（默认标准 + 差距清单）
- 配装推荐（单角色 Top-3 + LLM 解释）
- 队伍配置（Hungarian 全局武器分配）
- fasttext 粗分类（可选）/ 随机森林评分（可选）/ DeepSeek 解释（可选）
- 冒烟测试 + 端到端演示 + 启动脚本
